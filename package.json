{
  "name": "this-is-optimism",
  "version": "1.0.0",
  "description": "",
  "source": [
    "src/assets/scss/main.scss"
  ],
  "scripts": {
    "prod:parcel": "parcel build --no-cache",
    "prod:eleventy": "npx @11ty/eleventy",
    "bundle:parcel": "parcel watch --no-cache",
    "bundle:eleventy": "npx @11ty/eleventy --serve --quiet",
    "build": "npm-run-all --parallel prod:*",
    "dev": "npm-run-all --parallel bundle:*"
  },
  "keywords": [],
  "browserslist": "> 0.5%, last 3 versions, not dead, last 3 Safari major versions",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@parcel/transformer-sass": "^2.12.0",
    "npm-run-all": "^4.1.5",
    "parcel": "^2.12.0",
    "utopia-core-scss": "^1.0.1"
  }
}
