{% extends "layout.njk" %}

{% block inner %}

    <header class="o-wrap c-header">
        <h1>{{ title }}</h1>
        {% if subtitle %}
            <h2>
                <span>{{ event.date.name }}</span> <span>{{ event.location.name }}</span>
            </h2>
        {% endif %}
    </header>

    <main class="o-main o-wrap">
        <div class="c-content">
            {% block content %}{% endblock %}
        </div>
        {% block afterContent %}{% endblock %}
    </main>

    <footer class="o-wrap c-footer">
        This is Optimism is a Berst venture. We’re a product invention studio from nearby Brighton. After feeling rather frustrated at not being able to find an event we wanted to go to, we decided to try our own.
    </footer>

{% endblock %}